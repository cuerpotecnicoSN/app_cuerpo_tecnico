cat << 'INNER_EOF' > src/components/pro/MultimediaView.tsx
import React from 'react';
export default function MultimediaView(props: any) {
  return (
    <div className="p-8 text-center text-red-400 bg-red-900/20 border border-red-500/50 rounded-xl">
        <h2 className="text-xl font-bold mb-2">Componente Incompleto</h2>
        <p>Este componente (MultimediaView) se cortó al enviarlo por el chat.</p>
        <p>Por favor, sube el archivo original completo para restaurarlo.</p>
    </div>
  );
}
INNER_EOF

cat << 'INNER_EOF' > src/components/pro/PhysicalView.tsx
import React from 'react';
export default function PhysicalView(props: any) {
  return (
    <div className="p-8 text-center text-red-400 bg-red-900/20 border border-red-500/50 rounded-xl">
        <h2 className="text-xl font-bold mb-2">Componente Incompleto</h2>
        <p>Este componente (PhysicalView) se cortó al enviarlo por el chat.</p>
        <p>Por favor, sube el archivo original completo para restaurarlo.</p>
    </div>
  );
}
INNER_EOF

cat << 'INNER_EOF' > src/components/pro/WorkoutLogView.tsx
import React from 'react';
export default function WorkoutLogView(props: any) {
  return (
    <div className="p-8 text-center text-red-400 bg-red-900/20 border border-red-500/50 rounded-xl">
        <h2 className="text-xl font-bold mb-2">Componente Incompleto</h2>
        <p>Este componente (WorkoutLogView) se cortó al enviarlo por el chat.</p>
        <p>Por favor, sube el archivo original completo para restaurarlo.</p>
    </div>
  );
}
INNER_EOF
